# WinMagnifier 필수 패키지
PySide6>=6.6.0
numpy>=1.26.0

# WGC 는 순수 ctypes COM 구현 — winrt/pywin32 패키지 불필요.

# 선택: test_capture.py 에서 캡처 결과를 PNG 로 저장할 때만 사용
# Pillow>=10.0.0

# 개발·빌드용
pytest>=8.0.0
ruff>=0.4.0
pyinstaller>=6.0.0
